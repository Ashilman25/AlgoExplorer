VITE_API_BASE_URL=http://localhost:8000
# Production (Vercel): https://api.yourdomain.com
# In local dev, Vite proxies /api to this URL automatically.
# In production builds, this is baked into the bundle at build time.
